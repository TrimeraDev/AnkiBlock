/// Bundled image paths used across the app UI.
abstract final class AppAssets {
  /// Shield cutout (transparent). Regenerate via `python tool/generate_icons.py`.
  static const logo = 'lib/src/assets/logo_transparent.png';
}
