name: ankiblock
description: An Anki-compatible flashcard app with app blocking study gates
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # State & navigation
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7

  # Database
  drift: ^2.20.2
  sqlite3_flutter_libs: ^0.5.24

  # Storage & file
  path_provider: ^2.1.4
  path: ^1.9.0
  shared_preferences: ^2.3.2

  # Platform
  permission_handler: ^11.3.1

  # Card rendering (AnkiDroid hands us pre-rendered HTML)
  webview_flutter: ^4.10.0

  # Utils
  intl: ^0.19.0
  flutter_svg: ^2.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  drift_dev: ^2.20.2

flutter:
  uses-material-design: true
